# NOT THE MASTER INSTRUCTIONS

The real master instructions are injected by the IDE at the top of every prompt
(COPILOTWORKSPACE CONTEXT / IDESTATE CONTEXT blocks). They are NOT stored in Git.

DO NOT write instructions, directives, or content into this file. Keep it empty.
Never modify caching, service-worker, or deployment configuration.

Redirect to the injected master instructions in the prompt context only.
